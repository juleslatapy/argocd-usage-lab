apiVersion: v1
kind: Service
metadata:
  name: test-service
spec:
  ipFamilies:
  - IPv4
  ipFamilyPolicy: SingleStack
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: labfrontendapp
  type: ClusterIP
